# PRPilot

Small MVP for pasting a git diff and getting an AI-generated PR review.

## Local setup

1. Copy `.env.example` to `.env.local`
2. Set `OPENAI_API_KEY`
3. Run `npm install`
4. Run `npm run dev`
5. Open `http://localhost:3000`
